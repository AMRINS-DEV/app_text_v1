[project]
name = "agents-critic"
version = "0.1.0"
description = "critic-agent (§10.1): proposed signal + full evidence -> approve/reject + reasoning + adjusted confidence. The highest-leverage agent (§10.1) — sees counter-evidence and can veto."
requires-python = ">=3.11"
dependencies = ["agents-core", "agents-llm"]

[tool.uv.sources]
agents-core = { workspace = true }
agents-llm = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agents_critic"]
