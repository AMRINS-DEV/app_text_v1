[project]
name = "tradeos-agents-workspace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.3", "ruff>=0.8", "mypy>=1.13"]

[tool.uv]
package = false

[tool.uv.workspace]
members = ["packages/*"]
