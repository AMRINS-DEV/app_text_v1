import { Module } from "@nestjs/common";

/** Install, service control, health, logs (§11.1; Tauri-invoked via POST /api/system/install/mt5-bridge). Phase 4/11 scope. */
@Module({})
export class SystemModule {}
