# Kubernetes

Manifests for the stateless, horizontally-scalable agent layer (§16: "Agents
are stateless and horizontally scalable"). Phase 9+ scope — the core stays
on a dedicated VPS near the broker (§16), never in a cluster.
