[package]
name = "adapter-tradingview"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "TradingView adapter: webhook alerts as an external signal source + UDF datafeed server for charting. TV has no general order-execution API — this adapter is signal-only, never a Broker (§5.4). Phase 8 scope."

[dependencies]
domain.workspace = true
