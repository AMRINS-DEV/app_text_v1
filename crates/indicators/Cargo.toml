[package]
name = "indicators"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "O(1) incremental indicator math (§8.1). Same code compiles to WASM for the dashboard (packages/chart-engine) so the chart never disagrees with the engine."

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
