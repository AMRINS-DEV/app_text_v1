/**
 * §12.2 chart engine. Structure only for Phase 0 — `ChartHost`, `DataProvider`,
 * `LodManager`, `SyncBus`, the series primitives (PatternOverlay,
 * LevelOverlay, SignalMarker, PredictionCone, TradeOverlay, NewsMarker) and
 * the WASM indicator bindings (built from `crates/indicators`) are Phase 4
 * scope — see the design doc §12.2 for the target module layout.
 */
export const CHART_ENGINE_PHASE = "phase-0-stub" as const;
