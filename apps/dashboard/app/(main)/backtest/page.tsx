/** Run + compare strategy configs (§4, §15). Phase 3/7 scope. */
export default function BacktestPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Backtest</h1>
      <p className="mt-2 text-sm text-neutral-500">Run/compare NautilusTrader backtests (§15) — Phase 3/7 scope.</p>
    </main>
  );
}
