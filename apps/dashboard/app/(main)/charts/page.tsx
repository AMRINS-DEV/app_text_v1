/** Multi-chart workspace (§4, §12.2). Client component, WS-fed — Phase 4 scope. */
export default function ChartsPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Charts</h1>
      <p className="mt-2 text-sm text-neutral-500">
        Multi-chart workspace built on @tradeos/chart-engine (§12.2) — Phase 4 scope.
      </p>
    </main>
  );
}
