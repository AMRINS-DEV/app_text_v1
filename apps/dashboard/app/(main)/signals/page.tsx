/** Live signal feed + calibration curves (§4, §8.3, §14). Phase 5 scope. */
export default function SignalsPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Signals</h1>
      <p className="mt-2 text-sm text-neutral-500">
        Live feed + reliability diagrams (ECE, Brier score, §8.3) — Phase 5 scope.
      </p>
    </main>
  );
}
