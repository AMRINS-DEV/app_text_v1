/** News impact + graph explorer (§4, §12.4). Phase 5/6 scope. */
export default function NewsPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">News</h1>
      <p className="mt-2 text-sm text-neutral-500">Impact timeline + graph explorer (§12.4) — Phase 5/6 scope.</p>
    </main>
  );
}
