/** Keys, providers, pairs, risk profiles (§4, §11.1). Phase 4 scope. */
export default function SettingsPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Settings</h1>
      <p className="mt-2 text-sm text-neutral-500">
        Provider keys (write-only, masked reads — §13), pairs, risk profiles — Phase 4 scope.
      </p>
    </main>
  );
}
