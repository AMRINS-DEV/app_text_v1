/** Agent health, model routing, prompts, cost (§4, §10). Phase 5 scope. */
export default function AgentsPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Agents</h1>
      <p className="mt-2 text-sm text-neutral-500">Agent roster health, LLM router cost/latency (§10.2) — Phase 5 scope.</p>
    </main>
  );
}
