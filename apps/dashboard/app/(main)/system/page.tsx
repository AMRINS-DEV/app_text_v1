/** Install, services, logs, health (§4, §11.1). Phase 4/11 scope. */
export default function SystemPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">System</h1>
      <p className="mt-2 text-sm text-neutral-500">
        Install/service control/health/logs, Tauri-invoked (§11.2) — Phase 4/11 scope.
      </p>
    </main>
  );
}
