/** Positions, modes, kill switch (§4, §9.1, §9.5). Phase 4 scope. */
export default function TradingPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Trading</h1>
      <p className="mt-2 text-sm text-neutral-500">
        Positions, mode switcher (Normal/Aggressive/Scalp/Paper) and kill switch — Phase 4 scope.
      </p>
    </main>
  );
}
