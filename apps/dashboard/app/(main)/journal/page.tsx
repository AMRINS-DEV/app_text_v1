/** Trade history, per-trade replay (§4, §14). Phase 4 scope. */
export default function JournalPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Journal</h1>
      <p className="mt-2 text-sm text-neutral-500">Trade history + one-click replay bundle (§14) — Phase 4 scope.</p>
    </main>
  );
}
