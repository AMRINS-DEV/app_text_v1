/** Calendar -> detect -> render -> verify (§4, §12.3 state machine). Phase 5/6 scope. */
export default function PatternsPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Patterns</h1>
      <p className="mt-2 text-sm text-neutral-500">
        idle -&gt; configuring -&gt; detecting -&gt; rendering -&gt; reviewing (§12.3) — Phase 5/6 scope.
      </p>
    </main>
  );
}
